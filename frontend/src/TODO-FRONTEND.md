# Frontend Routing Update Progress

## Completed:
- [x] Updated App.js: `/` now shows MainPage.jsx, Login moved to `/login`

## Test:
```
cd frontend && npm start
```
Visit `http://localhost:3000` → MainPage loads first!

## Navigation Notes:
- MainPage → / (landing)
- Login → /login
- Dashboards protected by role

